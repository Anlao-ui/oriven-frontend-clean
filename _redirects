# ORIVEN — Render Static Site routing
# 200 = rewrite (serve file, keep URL)   302 = redirect (change URL)
# More specific rules must appear before the catch-all.

/app            /app.html       200
/signup         /index.html     200
/login          /index.html     200
/plan           /index.html     200
/onboarding     /app?tour=1     302
/*              /index.html     200
